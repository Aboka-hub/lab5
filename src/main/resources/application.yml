spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/student_api
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
